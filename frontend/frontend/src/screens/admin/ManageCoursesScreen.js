import PlaceholderNote from '../../components/common/PlaceholderNote';
import ScreenContainer from '../../components/common/ScreenContainer';

export default function ManageCoursesScreen() {
  return (
    <ScreenContainer title="Courses" subtitle="Create and edit the academy catalogue.">
      <PlaceholderNote>GET and POST /courses/</PlaceholderNote>
    </ScreenContainer>
  );
}
