import PlaceholderNote from '../../components/common/PlaceholderNote';
import ScreenContainer from '../../components/common/ScreenContainer';

export default function ChildrenListScreen() {
  return (
    <ScreenContainer title="My children" subtitle="Everyone you have registered with the academy.">
      <PlaceholderNote>Children come back with the parent dashboard payload.</PlaceholderNote>
    </ScreenContainer>
  );
}
